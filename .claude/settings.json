{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"$CLAUDE_PROJECT_DIR\" ] || exit 0; f=$(jq -r .tool_input.file_path); case \"$f\" in \"$CLAUDE_PROJECT_DIR\"/*) (cd \"$CLAUDE_PROJECT_DIR\" && pnpm exec biome check --fix --no-errors-on-unmatched \"$f\") ;; esac",
            "statusMessage": "Running biome check --fix"
          }
        ]
      }
    ]
  }
}
